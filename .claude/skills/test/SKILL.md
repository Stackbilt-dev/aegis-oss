---
name: test
description: Run the vitest test suite for aegis-oss
user_invocable: true
---

Run the test suite:

```bash
cd web && npm test 2>&1
```

If tests fail, show the failing test names and error messages. If tests pass, show the summary.

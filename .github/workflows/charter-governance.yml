name: Governance Check

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  pull-requests: write

jobs:
  governance:
    name: Charter
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Validate only this PR's commits. The default range (HEAD~5..HEAD)
      # re-checks commits already on main, so one squash commit whose trailer
      # GitHub moved out of the final paragraph failed every later PR.
      - name: Validate Commits
        run: npx charter validate --ci --format text --range "${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}"

      - name: Drift Scan
        run: npx charter drift --ci --format text

      - name: ADF Wiring & Pointer Integrity
        run: npx charter doctor --adf-only --ci --format text
        if: hashFiles('.ai/manifest.adf') != ''

      - name: ADF Evidence
        run: npx charter adf evidence --auto-measure --ci --format text
        if: hashFiles('.ai/manifest.adf') != ''

      - name: Audit Report
        run: npx charter audit --format json > /tmp/audit.json
        if: always()
